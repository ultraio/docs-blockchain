import{_ as e,c as t,o as a,N as s}from"./chunks/framework.dnQjGuIF.js";const w=JSON.parse('{"title":"How It Works","description":"","frontmatter":{"title":"How It Works","order":0,"outline":[0,4]},"headers":[],"relativePath":"products/ultra-web-wallet/how-it-works.md","filePath":"products/ultra-web-wallet/how-it-works.md","lastUpdated":null}'),i={name:"products/ultra-web-wallet/how-it-works.md"},n=s(`<h1 id="how-it-works" tabindex="-1">How It Works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How It Works&quot;">​</a></h1><p>The Ultra Web Wallet uses a popup and JSON-RPC messages over <code>window.postMessage</code> to handle dApp requests. The dApp never receives the user&#39;s private key.</p><h2 id="provider-selection" tabindex="-1">Provider selection <a class="header-anchor" href="#provider-selection" aria-label="Permalink to &quot;Provider selection&quot;">​</a></h2><p>When <code>UltraWalletSDK</code> is created without a fixed provider, it checks for the Ultra Wallet Browser Extension. It uses the extension when detected and otherwise creates the Web Wallet provider.</p><p>You can select the Web Wallet explicitly:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> wallet</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> UltraWalletSDK</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    environment: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;mainnet&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    provider: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;web&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p><code>environment</code> accepts <code>mainnet</code>, <code>testnet</code>, or a custom Web Wallet URL. It defaults to <code>mainnet</code>.</p><h2 id="connection-flow" tabindex="-1">Connection flow <a class="header-anchor" href="#connection-flow" aria-label="Permalink to &quot;Connection flow&quot;">​</a></h2><ol><li>A user action in the dApp calls <code>wallet.connect()</code>.</li><li>The SDK opens the configured Web Wallet URL in a popup and waits for its readiness handshake.</li><li>The Web Wallet authenticates the user through Ultra SSO.</li><li>For a new browser device, the wallet generates a key pair locally and registers the public key with the user&#39;s EBA account. Returning devices are activated using their existing local wallet data.</li><li>The user approves the dApp origin. That trusted origin is stored locally for the signed-in user, allowing later <code>connect()</code> calls to resolve without another approval prompt.</li><li>The wallet returns the blockchain account name and public key to the dApp.</li></ol><p>Passing a <code>nonce</code> to <code>connect()</code> also asks the wallet to sign that nonce. A nonce must begin with <code>message:</code>, <code>0x</code>, or <code>UOSx</code>. A nonce connection is not silently auto-approved, even for an already trusted origin.</p><h2 id="signing-flow" tabindex="-1">Signing flow <a class="header-anchor" href="#signing-flow" aria-label="Permalink to &quot;Signing flow&quot;">​</a></h2><p>For messages and transactions:</p><ol><li>The dApp calls <code>signMessage()</code> or <code>signTransaction()</code> from a user action.</li><li>The SDK opens or focuses the Web Wallet popup and sends the request after the readiness handshake.</li><li>The Web Wallet verifies that the dApp origin is connected and displays the request for approval.</li><li>After approval, signing happens in the Web Wallet using the locally stored encrypted key.</li><li>A transaction is signed and broadcast to the configured Ultra network by default. With <code>{ signOnly: true }</code>, the wallet returns the signed transaction without broadcasting it.</li><li>The result is returned to the dApp and the popup closes.</li></ol><p>Only one Web Wallet request can be pending from an SDK instance at a time.</p><h2 id="network-and-event-model" tabindex="-1">Network and event model <a class="header-anchor" href="#network-and-event-model" aria-label="Permalink to &quot;Network and event model&quot;">​</a></h2><p>The Web Wallet&#39;s network is selected when the SDK instance is created. To target a different environment, create a new <code>UltraWalletSDK</code> instance with a different <code>environment</code> value.</p><p>The Web Wallet transport is request/response only. Long-lived wallet events and runtime network switching are available through the browser-extension provider, not the Web Wallet provider.</p>`,17),o=[n];function l(r,d,c,h,p,u){return a(),t("div",null,o)}const g=e(i,[["render",l]]);export{w as __pageData,g as default};
