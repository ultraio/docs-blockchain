import{_ as e,c as a,o as t,N as o}from"./chunks/framework.DlKUEHLQ.js";const v=JSON.parse('{"title":"How does oracle contract calculate UOS conversion rate","description":"","frontmatter":{"title":"How does oracle contract calculate UOS conversion rate","order":1,"oultine":[0,5]},"headers":[],"relativePath":"blockchain/contracts/oracle-contract/how-does-oracle-contract-calculate-uos-conversion-rate.md","filePath":"blockchain/contracts/oracle-contract/how-does-oracle-contract-calculate-uos-conversion-rate.md","lastUpdated":null}'),n={name:"blockchain/contracts/oracle-contract/how-does-oracle-contract-calculate-uos-conversion-rate.md"},r=o('<h1 id="how-does-oracle-contract-calculate-uos-conversion-rate" tabindex="-1">How does oracle contract calculate UOS conversion rate <a class="header-anchor" href="#how-does-oracle-contract-calculate-uos-conversion-rate" aria-label="Permalink to &quot;How does oracle contract calculate UOS conversion rate&quot;">​</a></h1><p>On-chain UOS conversion oracle contract is required to process conversion rates from multiple possible registered exchanges and needs to provide accurate information and on top of that needs to be resilient to gaps in data and potential deviation.</p><h2 id="updating-feeddata-of-associated-exchange" tabindex="-1">Updating <code>feeddata</code> of associated exchange <a class="header-anchor" href="#updating-feeddata-of-associated-exchange" aria-label="Permalink to &quot;Updating `feeddata` of associated exchange&quot;">​</a></h2><p>When <code>pushrate</code> action is executed the oracle will update the <code>feeddata</code> table to add the new rate pushed by the exchange to the cache. Each exchange has a dedicated cache for rates which is later used for calculating the moving averages.</p><h2 id="calculating-moving-average-for-finalrates-table" tabindex="-1">Calculating moving average for <code>finalrates</code> table <a class="header-anchor" href="#calculating-moving-average-for-finalrates-table" aria-label="Permalink to &quot;Calculating moving average for `finalrates` table&quot;">​</a></h2><p>When UTC minute changes (so 60 seconds have passed) the oracle will finalize the accumulated rates when the next <code>pushrate</code> occurs.</p><p>During the finalization process the oracle will perform outlier detection for the rates received from exchanges and will calculate the weighted average based on the 24 hours trading volume of each exchange. Calculated value in this way is considered a 1 minute moving average and is inserted into the <code>finalrates</code> table for level 1 (minutes) and will update the <code>rolling_moving_average</code> stored there.</p><p>Oracle utilizes median absolute deviation algorithm to detect and reject outliers from the available set of conversion rates from different exchanges.</p><p>Another finalization step occurs at the end of each UTC hour and day. The oracle will use the moving average stored in <code>rolling_moving_average</code> for level 1 (minutes) of <code>finalrates</code> table to update the level 2 (hours) of <code>finalrates</code> table each hour. And it will do a similar update between level 2 (hours) and level 3 (days) at the end of each day. In the process the rates and <code>rolling_moving_average</code> of the <code>finalrates</code> table will be update.</p><p>For each exchange oracle stores 2 sets of 60 seconds worth of conversion rates. After the finalization the <code>feeddata</code> table will be updated to delete the oldest set of rates (located at the bottom half of the 120 elements array).</p><h2 id="updating-moving-averages-stored-in-finalaverage-table" tabindex="-1">Updating moving averages stored in <code>finalaverage</code> table <a class="header-anchor" href="#updating-moving-averages-stored-in-finalaverage-table" aria-label="Permalink to &quot;Updating moving averages stored in `finalaverage` table&quot;">​</a></h2><p>During each finalization step done by the oracle (either for minutes, hours or days levels) the oracle will additionally update moving averages stored in <code>finalaverage</code> table at the associated level. This means that when level 1 of <code>finalrates</code> is updated the <code>finalaverage</code> table at the scope of <code>MINUTES</code> will also be updated. Similar for 2 - <code>HOURS</code> and 3 - <code>DAYS</code>.</p><h2 id="handling-gaps-in-data" tabindex="-1">Handling gaps in data <a class="header-anchor" href="#handling-gaps-in-data" aria-label="Permalink to &quot;Handling gaps in data&quot;">​</a></h2><p>In case there were no rates provided to the oracle contract the next time any rate appears from any of the exchanges the oracle will perform the calculations based on the available rates only. In case there is not enough rates to calculate the 1 minute average - it will be skipped. Similar thing is done for hours and days - in case there is a large gap in data the moving averages will be left unchanged.</p><p>You can utilize the timestamp to figure out how old the moving average is and whether you can consider it up-to-date or outdated. For more details see <a href="./../../../tutorials/oracle/how-to-validate-and-refresh-moving-average.html#when-the-moving-average-can-be-considered-valid">this section</a></p><h2 id="updating-seconds-moving-averages-on-demand" tabindex="-1">Updating seconds moving averages on demand <a class="header-anchor" href="#updating-seconds-moving-averages-on-demand" aria-label="Permalink to &quot;Updating seconds moving averages on demand&quot;">​</a></h2><p>To trigger the update it is necessary to manually execute the <a href="./oracle-actions/calcsecma.html"><code>calcsecma</code></a> action.</p><p>When the action is executed it will retrieve the most recent rates from the <code>feeddata</code> table for all exchanges, will perform outlier detection and will calculate the weighted average based on the 24 hours trading volume for each exchange. This step is done for multiple <code>feeddata</code> entries based on the window size of the moving average that is updated.</p><p>After the update the moving average stored at the <code>SECONDS</code> level of the <code>finalaverage</code> table will be updated.</p>',19),c=[r];function i(l,d,s,h,g,u){return t(),a("div",null,c)}const p=e(n,[["render",i]]);export{v as __pageData,p as default};
